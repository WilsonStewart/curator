{
  "name": "curator",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "devDependencies": {
    "@commitlint/cli": "^20.3.0",
    "@commitlint/config-conventional": "^20.3.0",
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky"
  }
}